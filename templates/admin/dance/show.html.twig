{% extends 'base.html.twig' %}

{% block title %}Dance{% endblock %}

{% block body %}
    <h1>Dance</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ dance.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ dance.name }}</td>
            </tr>
            <tr>
                <th>BPM</th>
                <td>{{ dance.BPM }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_dance_index') }}">back to list</a>

    <a href="{{ path('app_dance_edit', {'id': dance.id}) }}">edit</a>

    {{ include('admin/dance/_delete_form.html.twig') }}
{% endblock %}
